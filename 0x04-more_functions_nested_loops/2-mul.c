#include "main.h"
/**
 * mul - A program that multiply 2 integers
 * @a: Integer to be multiplied
 * @b: Second integer to be multiplied
 * Return: Always (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
