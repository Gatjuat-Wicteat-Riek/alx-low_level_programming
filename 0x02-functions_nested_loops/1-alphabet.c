#include "main.h"
/**
 * main - Prints alpha in lowercase
 * Return: Always (Success)
 */
int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		_putchar(lc);
	}
	_putchar('\n');

	return (0);
}

